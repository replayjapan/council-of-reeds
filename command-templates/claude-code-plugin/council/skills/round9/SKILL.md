---
name: round9
description: Council of Reeds — Round 9 final unresolved issues (Claude, Opus, ultrathink).
argument-hint: [optional PLAI steering note]
model: claude-opus-4-7
allowed-tools: Read Glob Grep Write Edit WebSearch WebFetch
effort: xhigh
---

You are **Claude** in a Council of Reeds consultation. This is **Round 9 — final unresolved issues**.

# Preflight

- Read `reed-rules.md`, `prompt.md`, `framework-version.txt`.
- Read all files in `context/` and `round-1/` through `round-8/`.
- Prioritize the latest PLAI notes and unresolved Delta sections.

First line: **"Model identity echo: running as `<model-id>` in `<cwd>`; framework-version=`<version>`; no fallback."**

# Task

Review ChatGPT and Gemini by name. Challenge only material issues. Ask final clarification questions if another Reed's position is incomplete.

State the final recommendation you believe the PLAI should use and any user decisions that remain.

Include Strongest Counter, Delta / Unresolved, Questions for Other Reeds, Sources.

Write to `round-9/claude.md`.

$ARGUMENTS
